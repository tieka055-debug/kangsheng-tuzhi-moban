---
name: kangsheng-tuzhi-moban
description: 将供应链连接器工程 PDF 忠实重排为康生品牌图纸，保持型号、产品视图、尺寸公差、PCB、型号表和性能参数完整；用于单张验收和同族批量制作。
---

# 康生图纸：原图技术内容保全

唯一执行入口是 `scripts/kangsheng.py`。本版不带旧动态装箱、重绘数据表、截图拼参数、历史上传脚本。

## 不变量

- 原 PDF 只读。每张绑定原件 SHA256、记录身份、原图型号和零件号后缀；同系列不同型号是独立原件，禁止借用邻行参数。
- 所有产品视图、共享标注、PCB/剖视图、说明、性能、材质镀层、全部表行及脚注均为技术内容。搬运原矢量，不重打技术数值，不用 OCR 猜补。
- 型号表固定右上；性能固定其下的右侧中部；产品与 PCB 充分利用左、中、下部。不同源图应先核定内容组再放置，不直接复用另一型号裁切坐标。
- 只等比缩放完整组；L 形内容组的各矩形始终使用同一坐标变换。支持保留源图的既有彩色强调并转金色；黑/灰/蓝转品牌蓝，白色保留。
- 标题、系列名、单位、比例、页码、公差和投影法逐图照原图核对；字体必须覆盖标题字符。不要填默认产品参数，也不要删“系列”。
- 图纸内容优先于固定单页。多页输入会停止，需另外制定保全所有页的方案。

## 最短生产路径

1. 按 [manifest 规范](references/manifest.md) 用 `init` 建草稿；查看完整未裁切源页并确认型号。一次确定内容清单、源技术范围、所有组边界和位置，保存源核对证据。
2. 填写清单并记录 `inventory-hash`；审核者在 `review` 中绑定清单哈希和原件哈希。脚本不自动批准自己的清单。任一字段变动必须重审。
3. `build manifest.json --output OUTPUT --cache CACHE` 一次完成矢量复用、预览和自动核查。缓存按原件哈希/页/旋转/引擎版本去重。原始旋转先烘焙再裁切。
4. 只看一次低清全页、必要时放大局部；用同型号未裁切原图做独立逐块目检，特别核对边缘尺寸、各型号表行、单位和符号。自动 QA 不等于工程认证。
5. 用当前文件哈希填写 [输出验收证据](references/manifest.md#输出人工验收证据)，再 `verify --review`。未通过不发布，不通过降低阈值规避错误。

## 批次与飞书

批量接力先读 [批量执行与接力边界](references/batch-handoff.md)。型号/原图错配时跳过该记录、保留线上原状、记录证据并汇总，不阻塞其他已核实记录；禁止自动配邻行或静默修复原字段。首张获用户确认后才扩同族，缓存已合格源页，失败项单独重跑。不反复让模型读取历史对话或重算全部输出。处理飞书时读 [发布顺序](references/publishing.md)：用户授权覆盖也只针对指定行的输出附件字段，源 `2D图纸` 原件不改。此包不保存凭证、记录 ID、业务原件或上传代码。
